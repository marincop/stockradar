# GitHub recipes for marincop/stockradar

Connected GitHub login is **marincop**.

## Read

Use GitHub file/tree tools on `owner=marincop`, `repo=stockradar`.
Empty repo (no default branch yet) returns a 409. Create the first commit
onto `main`.

## Write

Push to `main`. Include `sha` when updating an existing file.
Commit messages: short, present tense.
Never force-push. Never rewrite history on `main`.

## Search fallback

Query: `user:marincop stockradar`

Do not use unscoped `stock` search. Other marincop repos that mention
"store" (POS, clock-in) are unrelated.
